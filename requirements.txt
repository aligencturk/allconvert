Flask==3.0.3
docx2pdf==0.1.8
pdf2docx==0.5.8
python-dotenv==1.0.1
pandas==2.2.2
openpyxl==3.1.2
pypiwin32==223
python-docx==1.1.2
Pillow==10.3.0
PyMuPDF==1.24.6
pydub==0.25.1
moviepy==1.0.3
xmltodict==0.13.0
dicttoxml==1.7.16
rarfile==4.2
yt-dlp==2025.6.25
requests==2.31.0
beautifulsoup4==4.12.3
imageio-ffmpeg==0.5.1

# Production iyileştirmeleri
psutil==5.9.8
Flask-Limiter==3.5.0

# Not: Aşağıdaki dönüştürücüler için ek bağımlılıklar gerekebilir.
# PDF'ten Excel'e -> tabula-py (Java kurulumu gerektirir)
# HTML'den PDF'e -> weasyprint (GTK+ kurulumu gerektirebilir)
# Ses/Video -> ffmpeg programının sistemde yüklü olması gerekir.
# RAR -> unrar programının sistemde yüklü olması gerekir. 